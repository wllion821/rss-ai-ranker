---
interface Props {
  score: number;
}

const { score } = Astro.props;

const getColor = (s: number) => {
  if (s > 80) return 'bg-green-100 text-green-700 border-green-200';
  if (s >= 60) return 'bg-yellow-100 text-yellow-700 border-yellow-200';
  return 'bg-gray-100 text-gray-700 border-gray-200';
};
---

<div class={`inline-flex items-center justify-center w-12 h-12 rounded-full border-2 font-bold text-lg ${getColor(score)}`}>
  {score}
</div>
